# =============================================================================
# CMMC Enclave Toolkit — Environment Configuration
# =============================================================================
# Copy this file to .env and fill in your values before running docker compose.
# NEVER commit your .env file to version control.
# =============================================================================

# ── VPN Gateway ────────────────────────────────────────────────────────────────
# Your server's public IP or DDNS hostname (required for WireGuard peer config)
VPN_SERVER_URL=your.server.ip.or.hostname

# Number of VPN peer configurations to generate (one per user needing CUI access)
VPN_PEERS=3

# WireGuard UDP port (default: 51820)
VPN_PORT=51820

# ── CUI Data Storage ──────────────────────────────────────────────────────────
# Host path for CUI document storage (will be encrypted Docker volume)
# This directory must exist and be owned by the cuiuser (UID 1000)
CUI_DATA_PATH=/opt/cui-enclave/data

# ── Audit Logging ─────────────────────────────────────────────────────────────
# Host path for audit log storage
AUDIT_LOG_PATH=/opt/cui-enclave/logs

# Log retention in days (NIST AU.3.045 — retain logs for defined period)
# CMMC Level 2 typically requires 90+ days
LOG_RETENTION_DAYS=90

# ── CUI Workstation ───────────────────────────────────────────────────────────
CUI_USER=cuiuser
TIMEZONE=America/New_York

# ── Organization Info (for reports) ──────────────────────────────────────────
ORG_NAME=Your Organization Name
CAGE_CODE=
